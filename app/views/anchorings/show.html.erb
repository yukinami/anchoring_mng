<% content_for :header do %>
  <h1>Anchoring Detail</h1>
<% end %>

<p id="notice"><%= notice %></p>

<%= render :partial => "anchoring", :object => @anchoring %>

<%= link_to 'Back', root_path %>
<%= link_to 'Confirm', confirm_anchoring_path(@anchoring)  %>
